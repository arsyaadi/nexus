# OKF Export Rule

When exporting or converting `.okf` documentation to Word (`docx`) or `docusaurus`:
1. Use `okf_export` tool from `okf` MCP server OR run CLI command: `node dist/index.js export . --format docx --out ./export`.
2. DO NOT use `pandoc`, `python`, or `libreoffice`.
